Prior Installation Requirements: Python 3x

Let us create a class for Discrete Random Variable:

class Discrete_Random_Variable:
    def __init__(self, x=0, y=1):
        self.variableType = ""
        self.low = x
        self.high = y
        return
    def draw(self, numberOfSamples):
        samples = np.random.random_integers(self.low, self.high, numberOfSamples)
        return samples
        
 print(samples)
